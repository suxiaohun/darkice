<div id="article-related">

  <div class="article-neighbour">
    <div class="article-prev-next">
      <%= link_to '下一页', {:action => 'next', :next_pos => @data[:next_pos], :id => @data[:id]},data: {turbo_method: :post,turbo_frame: 'content_frame'} %>
    </div>

    <div class="article-prev-next">
      <%= link_to '下一页', {:action => 'next', :next_pos => @data[:next_pos], :id => @data[:id]},data: {turbo_method: :post,turbo_frame: 'content_frame'} %>
    </div>

    <%= hidden_field_tag :curr_pos, @data[:curr_pos] %>
  </div>
</div>
<ul>
  <% @data[:lines].each do |line| %>
    <li><%= line %></li>
  <% end %>
</ul>

<div id="article-related">
  <div class="article-neighbour">
    <div class="article-prev-next">
      <%= link_to '下一页', {:action => 'next', :next_pos => @data[:next_pos], :id => @data[:id]},data: {turbo_method: :post,turbo_frame: 'content_frame'} %>
    </div>
    <div class="article-prev-next">
      <%= link_to '下一页', {:action => 'next', :next_pos => @data[:next_pos], :id => @data[:id]},data: {turbo_method: :post,turbo_frame: 'content_frame'} %>
    </div>
    <br>
    <br>

  </div>
</div>


