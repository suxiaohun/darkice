FROM ruby.base:2.6.5

# throw errors if Gemfile has been modified since Gemfile.lock
RUN bundle config --global frozen 1

WORKDIR /var/www/xiaopang

COPY Gemfile Gemfile.lock ./
RUN bundle install

#COPY . .
#
#CMD ["./your-daemon-or-script.rb"]